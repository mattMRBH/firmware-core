# Config HAL

This directory contains the shared `ConfigStore` interface and related public
config persistence types.

Current public HAL:

- `config_store.h` - typed key-value storage interface and common result enum
