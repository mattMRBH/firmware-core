# BSP Placeholder

This directory is reserved for the reference product BSP.

For now, the product only provides a thin `main/` so the repository has a real
ESP-IDF application root while the product-specific BSP shape is still being
designed.
